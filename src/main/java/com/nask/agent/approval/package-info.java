/**
 * Approval request API and service layer.
 *
 * <p>This package owns the pause/resume boundary for risky operations that must
 * be explicitly approved before the agent continues.</p>
 */
package com.nask.agent.approval;
