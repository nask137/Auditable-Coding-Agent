/**
 * Model-facing gateway contracts and DTOs.
 *
 * <p>The current implementations include a deterministic stub gateway and an
 * HTTP gateway that asks a real model for structured intent while the Runtime
 * keeps ownership of permissions, approvals, tool execution, and audit.</p>
 */
package com.nask.agent.llm;
