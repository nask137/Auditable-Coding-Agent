/**
 * Append-only audit event model and storage.
 *
 * <p>Audit events are the primary explanation trail for every task, run, step,
 * tool call, permission decision, approval, validation, and terminal outcome.</p>
 */
package com.nask.agent.audit;
